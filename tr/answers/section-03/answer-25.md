## 📘 Bölüm: Kontrol Akışı  
### 🔹 Kategori: Döngüler  
#### ✅ Cevap 25: `while` ile koşullu sayma

**Açıklama:**

`while` döngüsü, koşul doğru olduğu sürece devam eder.

```rust
fn main() {
    let mut n = 1;
    while n <= 5 {
        println!("n: {}", n);
        n += 1;
    }
}
```
