## 📘 Section: Lifetime System and HRTB Mastery  
### 🔹 Category: Using Higher-Ranked Trait Bounds (HRTB) with for<'a> syntax  
#### ❓ Question 716: Using Higher-Ranked Trait Bounds (HRTB) with for<'a> syntax

Write a Rust function that does the following:

- Accepts a closure or function as a parameter that can take a reference with any lifetime.
- Use the `for<'a>` syntax to specify a Higher-Ranked Trait Bound (HRTB).
- Provide an example showing how the function works.

🔧 **Task:** Create an example using HRTB and `for<'a>` to accept trait bounds independent of lifetime.
