## 📘 Section: Testing and Debugging  
### 🔹 Category: Test Modules  
#### ❓ Question 452: Grouping tests with `mod tests` and `cfg(test)`

Do the following:

- Define a function.
- Group tests in a separate `mod tests`.
- Use the `#[cfg(test)]` attribute to ensure tests only run during test builds.

🔧 **Task:** Organize tests by grouping them in a module.
